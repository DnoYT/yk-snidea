
---

### 📦 项目当前上下文与开发状态总结

**1. 项目基本信息**

* **项目名称**：`yk-snidea` (VS Code 侧边栏插件)
* **核心功能**：一个本地的提示词自动拼装工具。它允许开发者选择角色、开发规范、输入数据库信息，并通过富文本输入框描述需求，最后结合所选文件的**实际代码内容**，一键生成用于发送给大模型的标准化 Markdown 提示词。

**2. 整体架构设计（前后端分离）**

* **【后端】VS Code 插件核心 (TypeScript + esbuild)**：
  * 负责注册侧边栏视图 (`WebviewViewProvider`)。
  * 负责调用 VS Code 原生 API：读取当前工作区文件、执行 `findFiles` 全局模糊搜索、打开并在编辑器中定位文件。
  * 负责读取选中文件的代码内容，并拼装最终的提示词文本。
* **【前端】Webview UI (Vue 3 + Vite + JavaScript)**：
  * 独立在 `webview-ui` 目录下开发，使用 `yarn build --watch` 实时打包到插件的 `media` 目录下。
  * 采用单文件组件 (SFC) `App.vue` 开发，引入 Iconify 作为图标库。
  * 独立封装了 `src/utils/vscode.js` 用于处理与插件后端的消息通信 (`postMessage` & `onMessage`)。

**3. 已实现的核心高阶功能**

* **`@` 呼出文件搜索与插入**：在一个 `contenteditable="true"` 的富文本输入框内，用户输入 `@` 即可呼出当前工作区的文件下拉列表。
* **完善的键盘导航**：支持在下拉列表中使用 `↑`、`↓` 键选中文件，使用 `Enter` 或 `Tab` 键确认插入。
* **富文本行内 Tag**：选中文件后，会在输入框光标处插入一个 `contenteditable="false"` 的视觉 Tag，并支持使用 `Backspace` 键一键整块删除。
* **全局文件池管理**：选择的文件会同步显示在顶部的 Tag 列表中。自动处理了**同名文件问题**（同名文件会显示上级目录以作区分）。
* **便捷交互**：点击顶部 Tag 的文件名，可直接在 VS Code 中打开该文件；点击“X”可移除该文件。
* **一键生成与复制**：将表单数据与底层读取的代码文件组合成 Prompt，并在只读框中展示，支持一键复制到剪贴板。

**4. 必须严格遵守的开发规范**

* **代码规范**：**绝对禁止使用 `else` 语句**，必须优先使用“提前返回 (Early Return)”模式来编写干净的逻辑。
* **前端语言规范**：Webview 前端项目使用的是 **Vue 3 +纯 JavaScript**，不使用 TypeScript。
* **注释规范**：核心方法和复杂逻辑必须包含清晰的 JSDoc 或单行注释说明。

**5. 当前最新代码文件结构（摘要）**

* `src/extension.ts` (后端入口)
* `src/providers/HelloWorldViewProvider.ts` (后端视图与消息总线)
* `webview-ui/src/App.vue` (前端 UI 核心界面)
* `webview-ui/src/utils/vscode.js` (前端通信模块)

---
参考文件：
src\extension.ts

```
import * as vscode from 'vscode';
// 引入我们刚才写的 Provider 类
import { HelloWorldViewProvider } from './providers/HelloWorldViewProvider';
import { initIdeFolder } from './utils/fileSystem';

// 插件被激活时执行的函数
export function activate(context: vscode.ExtensionContext) {
 console.log('yk-side 插件已激活！');

 // 1. 实例化我们的 WebviewView 提供者
 const provider = new HelloWorldViewProvider(context.extensionUri);

 // 2. 注册这个提供者到 VS Code 窗口系统
 // 传入的 ID (HelloWorldViewProvider.viewType) 必须匹配 package.json 里的设定
 context.subscriptions.push(
  vscode.window.registerWebviewViewProvider(HelloWorldViewProvider.viewType, provider)
 );

 // 3. （可选）保留之前的命令注册逻辑
 let disposable = vscode.commands.registerCommand('yk-snidea.helloWorld', () => {
  vscode.window.showInformationMessage('Hello World from yk-side 命令!');
 });
 context.subscriptions.push(disposable);


 // 注册创建 .ide 文件夹的命令
 const initIdeDisposable = vscode.commands.registerCommand('yk-snidea.initIde', async () => {
  await initIdeFolder();
 });

 context.subscriptions.push(initIdeDisposable);
}

// 插件被卸载或停用时执行的函数
export function deactivate() { }
```

src\providers\HelloWorldViewProvider.ts

```
import * as vscode from 'vscode';
import * as fs from 'fs';
import * as path from 'path';

/**
 * 侧边栏视图提供者
 */
export class HelloWorldViewProvider implements vscode.WebviewViewProvider {
    public static readonly viewType = 'yk-snidea.helloView';

    constructor(
        private readonly _extensionUri: vscode.Uri,
    ) { }

    public resolveWebviewView(
        webviewView: vscode.WebviewView,
        context: vscode.WebviewViewResolveContext,
        _token: vscode.CancellationToken,
    ) {
        webviewView.webview.options = {
            enableScripts: true,
            localResourceRoots: [this._extensionUri]
        };

        webviewView.webview.html = this._getHtmlForWebview(webviewView.webview);

        webviewView.webview.onDidReceiveMessage(async (message) => {
            if (message.type === 'log') {
                console.log(`[前端日志 | ${message.tag}]`, message.data);
                return;
            }
            if (message.type === 'info') {
                vscode.window.showInformationMessage(message.value);
                return;
            }
            if (message.type === 'error') {
                vscode.window.showErrorMessage(message.value);
                return;
            }

            // 新增：处理点击 Tag 打开文件的请求
            if (message.type === 'openFile') {
                try {
                    const document = await vscode.workspace.openTextDocument(vscode.Uri.file(message.path));
                    await vscode.window.showTextDocument(document, { preview: false });
                } catch (error) {
                    vscode.window.showErrorMessage(`无法打开文件: ${message.path}`);
                }
                return;
            }

            if (message.type === 'searchFiles') {
                await this._handleSearchFiles(webviewView, message.keyword);
                return;
            }

            if (message.type === 'generate') {
                vscode.window.withProgress({
                    location: vscode.ProgressLocation.Notification,
                    title: "正在解析文件并生成提示词...",
                    cancellable: false
                }, async () => {
                    const prompt = await this._buildPrompt(message.data);
                    webviewView.webview.postMessage({ type: 'renderResult', value: prompt });
                });
                return;
            }
        });
    }

    /**
     * 处理文件搜索请求
     * @param {vscode.WebviewView} webviewView - Webview 实例
     * @param {string} keyword - 用户在 @ 后面输入的关键字
     */
    private async _handleSearchFiles(webviewView: vscode.WebviewView, keyword: string): Promise<void> {
        const workspaceFolders = vscode.workspace.workspaceFolders;

        // 提前返回：如果没有打开工作区，直接返回空列表
        if (!workspaceFolders || workspaceFolders.length === 0) {
            webviewView.webview.postMessage({ type: 'searchResults', files: [] });
            return;
        }

        try {
            // 构建搜索模式，例如用户输入 main，则搜索 **/*main*
            // 如果 keyword 为空，则搜索所有文件 **/*
            const searchPattern = keyword ? `**/*${keyword}*` : `**/*`;
            // 排除 node_modules, dist, .git 等无关文件夹
            const excludePattern = '**/{node_modules,.git,dist,out,.vscode}/**';

            // 限制最多返回 20 条结果，保证性能
            const uris = await vscode.workspace.findFiles(searchPattern, excludePattern, 20);

            // 格式化数据，提取绝对路径和相对路径
            const files = uris.map(uri => ({
                fsPath: uri.fsPath,
                relativePath: vscode.workspace.asRelativePath(uri, false)
            }));

            webviewView.webview.postMessage({ type: 'searchResults', files });
        } catch (error) {
            console.error('[后端报错] 搜索文件失败:', error);
            webviewView.webview.postMessage({ type: 'searchResults', files: [] });
        }
    }

    /**
     * 读取外部 HTML 文件并处理资源路径转换
     * @param {vscode.Webview} webview - 当前的 Webview 实例
     * @returns {string} 处理后的 HTML 字符串
     */
    private _getHtmlForWebview(webview: vscode.Webview): string {
        // 引用 Vite 构建出的静态资源
        const scriptUri = webview.asWebviewUri(vscode.Uri.joinPath(this._extensionUri, 'media', 'index.js'));
        const styleUri = webview.asWebviewUri(vscode.Uri.joinPath(this._extensionUri, 'media', 'index.css'));

        return `<!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="${styleUri}">
        <title>YK Snidea</title>
    </head>
    <body>
        <div id="app"></div>
        <script src="${scriptUri}"></script>
    </body>
    </html>`;
    }
    /**
      * 核心业务逻辑：根据前端传递的参数和文件路径数组，组装提示词
      * @param {any} data - 前端表单数据
      * @returns {Promise<string>} 拼接好的完整提示词
      */
    private async _buildPrompt(data: any): Promise<string> {
        let filesContent = '';

        // 确保传递过来的是数组，且不为空
        if (data.files && Array.isArray(data.files) && data.files.length > 0) {
            for (const finalPath of data.files) {
                const fileName = path.basename(finalPath);
                try {
                    const fileUri = vscode.Uri.file(finalPath);
                    const document = await vscode.workspace.openTextDocument(fileUri);
                    const text = document.getText();

                    // 【关键点】：使用 += 累加多个文件的内容，每个文件用 Markdown 代码块包裹
                    filesContent += `\n### 参考文件: ${fileName}\n\`\`\`\n${text}\n\`\`\`\n`;
                    console.log(`[后端运行] 成功将文件写入提示词: ${fileName}`);
                } catch (error) {
                    filesContent += `\n### [读取失败]: 无法访问文件 ${finalPath}\n`;
                    console.error(`[后端报错] 无法读取文件: ${finalPath}`, error);
                }
            }
        }

        return `
# 角色与技能
${data.skills}

# 开发规范
${data.rules}

# 数据库参考
${data.dbInfo || '无'}

# 核心参考代码
${filesContent || '未提供参考文件'}

# 本次开发需求
${data.requirement || '未提供具体需求'}

---
请严格遵守上述规范，结合提供的上下文，输出包含完整代码文件和 SQL 的解决方案。
        `.trim();
    }
}
```

**接下来的任务是：**
---

 首先我们先来解决后端的项目问题

 1. 我希望把后端项目中的代码按需求拆分为多个文件，比如渲染视图就在试图文件中，读取文件就在文件读取文件中，这样代码看起来更清晰

* sql数据库结构读取，数据读取，数据库注册，链接
* 提示词输出、skill 拼接、文件拼接等

 1. 我和你说一下我这个项目的原理，我这个项目是为了方便用户快速生成ai提示词，提示词中包括数据库的结构、原文件、提示词的设置

* 比如，我提前写了2个规范“不要使用else”、“减少语法糖使用”，然后我设置一个规范叫“uniapp”，然后我可以把“减少语法糖使用”绑定到uniapp规范下，然后我选择uniapp规范，就可以自动生成提示词了，全局数据
* 比如， 我可以先设置数据库的账号密码，然后我在输入框内收入@就会去搜索数据库中的表列表，我可以选择我想要的表，这个时候就会把结构生成到提示词，一个工作区绑定一个数据库

1. 你看看怎么设计项目结构，项目功能好一些，我们先设计后端
2. src\providers\HelloWorldViewProvider.ts 文件名是不合适的，想要更改
